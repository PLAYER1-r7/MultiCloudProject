# タスク契約テンプレート

実装を開始する前にこのテンプレートを埋めます。

## 流用ルール

- 編集開始前に、下の fenced テンプレートをそのままコピーして使う
- 各プレースホルダは同じ行で置き換え、本当に不要な行だけ削除する
- 受け入れ条件と検証コマンドは、別のエージェントが再解釈なしで引き継げる粒度まで具体化する
- 独立に確認できる参照が複数ある場合は、read-only の文脈収集を 1 行で要約して契約に残す
- 編集前にファイル変更の可能性がある場合は、読み直したうえで最終的な編集対象を契約へ反映し、古いメモを前提にしない
- browser-facing な portal copy や他の公開向け文面を変更する作業では、scope が `local-only` なのか live reflection を含むのかを明記する
- 作業が follow-up issue chain を新規作成または継続する可能性がある場合は、次の Issue を作る前に `Terminal condition` を埋める
- reviewer と approval owner の境界が関わる場合は、`依頼者`、`期待する価値`、引き継ぎ系のスコープ記述を埋める前に `ROLE_HANDOFF_OWNERSHIP.md` に合わせる
- 通常の実装だけでなく、レビュー指摘への是正、文書だけの修正、Final Review の取りまとめ作業にもこのテンプレートを使う

## 最小着手ルール

- すべての詳細が固まる前に作業を始める必要があっても、最初の編集前に必ずタスク契約を作る
- 最低でも `タイトル`、`依頼者`、`対象アプリ`、`対象環境`、`解決する問題`、`含むもの`、`含まないもの`、受け入れ条件 1 つ、検証コマンド 1 つを埋める
- read-only の文脈収集を並列化した場合は、そのことを `アプローチ` に短く記録する
- browser-facing な portal copy や他の公開向け文面を変更する作業では、live reflection を含むかどうかと、その証拠を何で示すかも書く
- 作業が follow-up issue に分岐しうる場合は、最初の child を作る前に `Terminal condition` も埋める
- 未確定値は空欄のままにせず、明示的に `TBD` と書く
- 作業が最初の狭い想定より広がった場合は、引き継ぎ前に契約を拡張する

## 最小着手ブロック

速度優先で、フルテンプレートを最初から埋めると安全な着手が遅れる場合にだけ使う。

```text
タスク契約

メタデータ
- タスク ID: TBD
- タイトル:
- 依頼者:
- 担当者: TBD
- 対象アプリ:
- 対象環境:
- 優先度: TBD
- 前提 Issue: (このコントラクトを有効化する前に CLOSED であることが必須の Issue 番号、または「なし」)

目的
- 解決する問題:
- 期待する価値: TBD
- Terminal condition: TBD

スコープ
- 含むもの:
- 含まないもの:
- 編集可能パス: TBD
- 制限パス: TBD

受け入れ条件
- [ ] 条件 1:

実装計画
- 変更見込みファイル: TBD
- アプローチ: TBD
- 編集前の再読トリガー: TBD
- 採用しなかった代替案と理由: TBD

検証計画
- 実行するテスト:
- 確認するログ/メトリクス: TBD
- 未実行項目と理由: TBD
- 失敗時の切り分け経路: TBD

リスクとロールバック
- 主なリスク: TBD
- 影響範囲: TBD
- 緩和策: TBD
- ロールバック手順: TBD
```

## コピー用テンプレート

```text
タスク契約

メタデータ
- タスク ID:
- タイトル:
- 依頼者:
- 担当者:
- 対象アプリ:
- 対象環境:
- 優先度:
- 前提 Issue:

目的
- 解決する問題:
- 期待する価値:
- Terminal condition:

スコープ
- 含むもの:
- 含まないもの:
- 編集可能パス:
- 制限パス:

受け入れ条件
- [ ] 条件 1:
- [ ] 条件 2:
- [ ] 条件 3:

実装計画
- 変更見込みファイル:
- アプローチ:
- 編集前の再読トリガー:
- 採用しなかった代替案と理由:

検証計画
- 実行するテスト:
- 確認するログ/メトリクス:
- 未実行項目と理由:
- 失敗時の切り分け経路:

リスクとロールバック
- 主なリスク:
- 影響範囲:
- 緩和策:
- ロールバック手順:
```

上記のブロックをそのままコピーし、Markdown の見出し形式に変換しないでください。

## 記入例

```text
タスク契約

メタデータ
- タスク ID: AGENT-104
- タイトル: Issue #451 exam-solver staging の callback と timeout 契約更新
- 依頼者: exam-solver-reviewer
- 担当者: AI agent
- 対象アプリ: exam-solver
- 対象環境: staging
- 優先度: 高

目的
- 解決する問題: exam-solver のフロントエンドと API 作業で callback と timeout のガイダンスが揃っていない
- 期待する価値: exam-solver-reviewer への引き継ぎと exam-solver-approval-owner への承認依頼を行う前に、deploy-exam-solver-aws.yml 対象の staging 挙動を安全に更新できる
- Terminal condition: callback と timeout の契約文言が 1 つの正本で固定され、この更新のための追加 child issue が不要になった時点で完了とする

スコープ
- 含むもの: Issue #451 に対する docs_agent の callback と timeout 判断文言、および exam-solver-reviewer 向け引き継ぎメモ
- 含まないもの: 実行コードの変更
- 編集可能パス: docs_agent/20_MANUAL_DEPLOY_DECISION_CRITERIA.md
- 制限パス: services/, infrastructure/
```

## 実行記録との接続

この契約は、08 と 14-32 で使う共通実行記録の元文書として使います。

- 実行記録の `Scope` には、目的とスコープを要約して入れる
- `Actions taken` には、最終的に実行した実装計画を書く
- `Evidence` には、検証計画、実際の検証結果、未実行の関連確認を書く
- `Risks or blockers` には、リスクとロールバック情報、および作業中に見つかった阻害要因を書く
- `Next action` には、完了、エスカレーション、引き継ぎ、または次の実装ステップを書く

## 完了時の引き渡しルール

この契約から作業を始めた場合、終了時は `08_ESCALATION_AND_HANDOFF.md` の正規 Execution Record 形式を使って閉じます。
